<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
  <!-- KWC 파일 안내 / KWC file guide
       이 POM은 kwc-core/pom.xml의 Maven module 관계, Java toolchain/dependency, 최종 artifact 구성을 정의한다.
       This POM defines Maven module relationships, Java toolchain/dependencies, and final artifact composition for kwc-core/pom.xml.
       shaded Bukkit JAR에 포함될 dependency와 loader-neutral core의 compile boundary를 바꿀 때는 45-target release 검증과 함께 확인한다.
       When changing shaded-Bukkit dependencies or loader-neutral core compile boundaries, validate them together with the 45-target release matrix. -->


    <parent>
        <groupId>dev.kokoto</groupId>
        <artifactId>kokoto-webchat-parent</artifactId>
        <version>5.3.0</version>
    </parent>

    <artifactId>kwc-core</artifactId>
    <packaging>jar</packaging>
    <name>KOKOTO WebChat Core</name>
    <description>Platform-neutral chat/session/security, persistence, relay, complete HTTP/SSE endpoint orchestration, game-platform contracts, and Web Push services.</description>
    <dependencies>
        <dependency>
            <groupId>org.xerial</groupId>
            <artifactId>sqlite-jdbc</artifactId>
            <version>3.53.2.0</version>
        </dependency>
    </dependencies>

</project>
